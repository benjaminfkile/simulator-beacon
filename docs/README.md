# Design documents

- `simulator-beacon.md`: the technical design of this repository. Belongs here; edit it here.
- `DESIGN.md` and `contracts.md`: byte-identical copies of the originals in `wmsfo-api/docs`. Never edit them here; change the original and recopy.

Section references such as "contracts 9.2" point into `contracts.md`.
